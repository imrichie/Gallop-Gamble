#ifndef UTILITY_H
#define UTILITY_H

// Randomly shuffles an array of integers
void shuffleArray(int *array, int size);

// Validates user input for horse numbers
int validateHorseNumbers(int *horses, int count, int maxHorseNumber);

#endif // UTILITY_H
